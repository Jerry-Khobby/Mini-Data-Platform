[project]
name = "mini-data-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "faker>=40.4.0",
    "minio>=7.2.20",
    "pandas>=3.0.1",
    "python-dotenv>=1.2.1",
    "sqlalchemy==1.4.54",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
